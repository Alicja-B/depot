<div class="col-xs-12 col-md-3" >
<div class="thumbnail">
  <%= image_tag(product.image_url, :class=>"image-responsive") %>
  <div class="caption">
    <h3><%= product.title %></h3>
    <p><%= button_to 'Show', product, class: 'btn btn-info', method: :get %> <%= button_to 'Add to Cart', line_items_path(product_id: product), class: 'btn btn-info', remote: true %>
</p>
 </div>
  </div>
</div>
